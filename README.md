# Mike Monteith's dotfiles

This project is a way of tracking changes to dotfiles, configs, profiles and global dependencies.
It allows quick installation of dev tooling on a fresh MacOS machine.

Run `./install.sh`
